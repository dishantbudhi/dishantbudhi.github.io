# Music artwork

Add the square cover image for the featured Spotify playlist here as:

`2wo.webp`

The card crops the image to a square automatically. If the file is missing, the site shows a styled 2WO fallback instead of a broken image.
