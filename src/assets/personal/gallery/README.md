# Personal gallery

Drop `.avif`, `.jpeg`, `.jpg`, `.png`, or `.webp` files into this folder. They are
automatically discovered and displayed as a responsive square grid on the Personal
page. Filenames become accessible image labels, so descriptive names work best.

When this folder contains no images, the fallback images configured in
`public/data/site.json` are shown instead.
