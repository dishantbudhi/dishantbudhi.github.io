/** Public JSON files that can be edited without touching React components. */
export const contentPaths = {
  site: '/data/site.json',
  resume: '/data/resume.json',
  projects: '/data/projects.json',
} as const
